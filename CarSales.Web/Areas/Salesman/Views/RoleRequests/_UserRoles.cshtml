@model IEnumerable<RoleListModel>
<h2 class="text-center">Current Roles</h2>
<table class=" table table-bordered  table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                Description
            </th>
            <th>

            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var role in Model)
        {
            <tr>
                <td width="30%">
                    @role.Name
                </td>
                <td width="50%">
                    @role.Description
                </td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        @if (role.Name != "Owner")
                        {
                            <a asp-area="Salesman" asp-controller="RoleRequests" asp-action="LeaveRole" asp-route-roleName="@role.Name" class="btn btn-danger mx-2">
                                <i class="bi bi-x-octagon-fill"></i>
                                Leave Role
                            </a>
                        }
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>