@model ReviewListModel

<article class="col mt-3 mb-3 text-break text-white" style="max-height:300px">
    <div class="d-flex justify-content-evenly container review-container w-100 h-100 basic-transition custom-border-white-blue p-0">

        <div class="text-center w-50 bg-light bg-opacity-25">
            <img class="w-100 h-100" src="@Model.VehicleImageUrl" alt="vehicleImg" />
        </div>
        <div class="d-flex flex-column justify-content-between w-50 bg-dark bg-opacity-10 ">
            <h3 class="text-center mt-2 mb-2">
                @Model.VehicleName
            </h3>
            <div class="mt-2 mb-2 container">
                <h6 class="text-center text-wrap">
                    @Model.Title
                </h6>
            </div>
            <div class="text-center mt-2 mb-2">
                <a class="btn btn-primary" asp-area="Importer" asp-controller="Reviews" asp-action="Details" asp-route-id="@Model.Id">Read More</a>
            </div>
        </div>
    </div>

</article>
